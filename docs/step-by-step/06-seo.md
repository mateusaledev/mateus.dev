# Fase 6 — SEO

## O que foi feito

- `generateMetadata` nas páginas principais
- `metadataBase` e favicon em `src/app/layout.tsx`
- `sitemap.xml` dinâmico (`src/app/sitemap.ts`)
- `robots.txt` (`src/app/robots.ts`)
- Open Graph básico em posts e layout

## Ajuste pós-deploy

Atualize a URL em:

- `src/app/layout.tsx` (`metadataBase`)
- `src/app/sitemap.ts` e `src/app/robots.ts`

Quando configurar domínio customizado na Vercel.
